[project]
name = "photo-tools"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pillow-heif>=1.3.0",
    "requests>=2.33.1",
    "numpy>=2.0",
]

[project.scripts]
photo-tools = "cli:main"
